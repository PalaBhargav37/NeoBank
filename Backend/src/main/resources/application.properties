# NeoBank Application Config

# Server
server.port=8080

# Database
spring.datasource.url=jdbc:mysql://localhost:3306/neobank_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=bhargav
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.open-in-view=false

# JWT
app.jwt.secret=NeoBankSuperSecretKeyForJWTAuthentication2024VeryLongAndSecure
app.jwt.expiration=86400000

# CORS
app.cors.allowed-origins=http://localhost:4200

# File Upload
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
